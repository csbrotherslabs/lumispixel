(function(){
  const layer=document.querySelector('[data-financial-layer]'),form=layer?.querySelector('[data-financial-form]');if(!layer||!form)return;
  let action='';const titles={payment:['Record payment','Apply a full or partial payment safely.'],refund:['Issue refund','Return funds from an eligible completed payment.'],credit:['Add credit','Create reusable value for a client.']};
  const close=()=>{layer.hidden=true;document.body.style.overflow='';form.reset();form.querySelector('[name=confirmed]').value='no'};
  document.querySelectorAll('[data-financial-open]').forEach(b=>b.addEventListener('click',()=>{action=b.dataset.financialOpen;layer.hidden=false;document.body.style.overflow='hidden';form.querySelector('[name=submission_key]').value=crypto.randomUUID();layer.querySelector('[data-action-title]').textContent=titles[action][0];layer.querySelector('[data-action-subtitle]').textContent=titles[action][1];layer.querySelectorAll('[data-action-fields]').forEach(x=>{x.hidden=x.dataset.actionFields!==action;x.querySelectorAll('input,select,textarea').forEach(i=>i.disabled=x.hidden)});layer.querySelector('[data-action-confirm]').hidden=true;layer.querySelector('[data-submit]').textContent='Review action'}));
  layer.querySelectorAll('[data-financial-close]').forEach(b=>b.addEventListener('click',close));
  const payment=layer.querySelector('[data-action-fields=refund] select[name=payment]');payment?.addEventListener('change',()=>{const o=payment.selectedOptions[0],v=o?.dataset.refundable||'—';layer.querySelector('[data-refundable]').textContent=v;const amount=layer.querySelector('[data-action-fields=refund] input[name=amount]');amount.max=o?.dataset.refundable||''});
  form.addEventListener('submit',async e=>{e.preventDefault();const confirm=form.querySelector('[name=confirmed]'),box=layer.querySelector('[data-action-confirm]');if(confirm.value!=='yes'){if(!form.reportValidity())return;confirm.value='yes';box.hidden=false;layer.querySelector('[data-confirm-copy]').textContent=`Please verify the ${action} details. Financial balances will update immediately.`;layer.querySelector('[data-submit]').textContent=`Confirm ${action}`;return}const submit=layer.querySelector('[data-submit]'),error=layer.querySelector('[data-form-error]');submit.disabled=true;submit.textContent='Saving…';error.hidden=true;try{const response=await fetch(form.dataset.endpoint.replace('__action__',action),{method:'POST',body:new FormData(form),headers:{'X-Requested-With':'XMLHttpRequest'}}),data=await response.json();if(!response.ok){error.textContent=Object.values(data.errors||{error:data.error||'The action failed.'}).join(' ');error.hidden=false;confirm.value='no';box.hidden=true;submit.textContent='Review action';return}window.location.assign(`${window.location.pathname}?range=all_time&${action}=${data.record_id}`)}catch(_){error.textContent='The action could not be completed. No additional changes were made.';error.hidden=false}finally{submit.disabled=false}});
})();
