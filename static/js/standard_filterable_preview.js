(function(){'use strict';const grid=document.querySelector('[data-photo-grid]');const slider=document.querySelector('[data-grid-density]');const output=document.querySelector('[data-grid-output]');document.querySelectorAll('[data-filter]').forEach(function(btn){btn.addEventListener('click',function(){document.querySelectorAll('[data-filter]').forEach(b=>b.classList.remove('is-active'));btn.classList.add('is-active');const filter=btn.dataset.filter;grid.querySelectorAll('[data-category]').forEach(function(photo){photo.hidden=filter!=='all'&&photo.dataset.category!==filter;});});});if(slider){slider.addEventListener('input',function(){grid.style.setProperty('--columns',slider.value);output.textContent=slider.value+' columns';});}const use=document.querySelector('[data-use-design]');if(use){use.addEventListener('click',function(){window.location.href=use.dataset.builderUrl;});}})();